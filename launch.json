{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Edge against localhost",
      "type": "edge",
      "request": "launch",
      "url": "http://localhost:8080",  // Adjust URL if needed
      "webRoot": "${workspaceFolder}"
    }
  ]
}
